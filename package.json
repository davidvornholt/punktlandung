{
  "name": "punktlandung",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "standards": "standards",
    "check": "bun test scripts/repository-artifacts.test.ts && standards check && turbo run lint check-types test build test:a11y --output-logs=errors-only",
    "check:fix": "bun test scripts/repository-artifacts.test.ts && standards check && turbo run lint:fix check-types test build test:a11y --output-logs=errors-only",
    "test:a11y": "turbo run test:a11y",
    "dev": "turbo run dev --filter=@punktlandung/web",
    "build": "turbo run build --filter=@punktlandung/web",
    "start": "turbo run start --filter=@punktlandung/web"
  },
  "engines": {
    "node": "26",
    "bun": "1.4.2"
  },
  "packageManager": "bun@1.4.2",
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@davidvornholt/standards": "0.27.0",
    "turbo": "^2.11.4",
    "typescript": "7.0.2"
  }
}
