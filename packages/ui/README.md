# @punktlandung/ui

Shared design foundation for Punktlandung: the Refined Heritage theme tokens (`./theme.css`, consumed by the app's Tailwind entry) and the shared motion constants (`./motion`). See the root `DESIGN.md` for the design contract.

This package consumes no configuration values or secrets.
